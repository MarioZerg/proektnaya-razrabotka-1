-- Возвращаем дату начала автоматического расчёта после проверки.
--
-- Проверено: к выплате идёт полная сумма процента от перечисленного
-- (1 433 356 ₽ × 3,5% = 50 167 ₽), повторного удержания за возвраты нет.
--
-- Пробное начисление за 17–23.08 аннулируем: эту неделю владелец сверяет
-- и оплачивает вручную, как и все предыдущие.
UPDATE manager_commission_settings SET accrue_from = DATE '2026-08-24';

UPDATE manager_accruals
SET status = 'cancelled',
    cancelled_at = now(),
    cancel_reason = 'Пробный расчёт при настройке. Эта неделя оплачивается вручную'
WHERE user_id = 32 AND period_start < DATE '2026-08-24';
