-- Возвращаем обход заказов OZON в начало списка.
-- После смены фильтра статусов позиция ушла далеко вперёд, и загрузка упиралась
-- в пустые страницы, не доходя до заказов, ожидающих сборки.
UPDATE system_settings SET value = '0', updated_at = now()
WHERE key = 'ozon_sync_offset';
