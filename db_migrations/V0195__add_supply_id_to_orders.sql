-- Прямая связь заказа на пошив с поставкой FBO.
-- Раньше связь держалась только на номере заказа (номер заявки OZON в начале строки).
-- Из-за этого нельзя было ни надёжно посчитать, что уже сшито по поставке, ни догрузить
-- в состав товар вручную: у ручного заказа номер вида 00000-01, префикса заявки в нём нет.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS supply_id INTEGER;

CREATE INDEX IF NOT EXISTS idx_orders_supply_id ON orders(supply_id);