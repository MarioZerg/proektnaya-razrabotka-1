-- Отключаем временные учётки, заведённые при проверке автоподстановки кода входа.
-- Удалять записи нельзя, поэтому просто выключаем их: войти под ними невозможно.
UPDATE users
SET is_active = false,
    max_user_id = NULL,
    phone = NULL,
    full_name = 'Отключено (тест входа)'
WHERE max_user_id IN ('999000111', '999777333');
