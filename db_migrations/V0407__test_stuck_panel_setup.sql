-- Проверка плашки «Зависли после отмены» на реальных данных.
--
-- Временно приводим вещь 118 в то самое состояние, которое плашка должна ловить:
-- заказ отменён на маркетплейсе, вещь числится «в сборке» с наклеенным ярлыком
-- отправления и при этом не закреплена ни за одним живым заказом.
--
-- Сразу следующей миграцией вернём её обратно на полку — это только тест механизма.
UPDATE t_p86119184_proektnaya_razrabotk.goods_warehouse
SET status = 'awaiting_supply',
    shipping_labeled_at = now()
WHERE id = 118 AND status = 'in_stock';
