-- Цены возвращены к состоянию 20 августа: сбойный подъём отменён полностью.
-- Все прогоны робота помечаем проверочными, чтобы счётчик пути к цели
-- стартовал с нуля — иначе робот считал бы уже пройденными проценты,
-- которых на витрине больше нет.
UPDATE price_robot_runs SET decision = 'test' WHERE decision <> 'test';

-- Робот остаётся выключенным и в наблюдении: включает владелец сам,
-- когда решит начать подъём.
UPDATE price_robot_settings
SET is_active = false, dry_run = true
WHERE marketplace_code = 'ozon';