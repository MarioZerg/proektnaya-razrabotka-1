-- Перезагружаем возвраты Яндекса с правильными полями.
--
-- При первой загрузке артикул читался из offerId, но у Яндекса он называется shopSku —
-- поэтому товар не находился в справочнике и названия остались пустыми. Повторная
-- синхронизация существующие записи не переписывает (обновляет только статус), поэтому
-- помечаем эти заявки отклонёнными: следующая загрузка заведёт их заново, уже с товаром.
-- Ни одна из них ещё не обрабатывалась кладовщиком, терять нечего.
UPDATE t_p86119184_proektnaya_razrabotk.marketplace_returns
SET external_id = external_id || '-old', status = 'rejected'
WHERE marketplace = 'Yandex' AND status = 'new' AND marketplace_item_id IS NULL;
