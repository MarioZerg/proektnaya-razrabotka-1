-- Все прогоны до этого момента были отладочными: цены на витрине не менялись
-- ни разу. Помечаем их проверочными, чтобы счётчик пути к цели стартовал
-- с нуля — иначе робот считал бы уже пройденными полпроцента, которых на
-- витрине никогда не было.
UPDATE price_robot_runs SET decision = 'test' WHERE decision <> 'test';