-- Скрываем тестовое сообщение, отправленное при проверке чата: сотрудники не должны
-- видеть в ленте служебную запись «Проверка связи» от имени Марии Ануфриевой.
-- Строку не убираем из базы — чат вообще не стирает сообщения, только скрывает.
UPDATE chat_messages
SET hidden_at = now()
WHERE id = 1 AND text = 'Проверка связи';
