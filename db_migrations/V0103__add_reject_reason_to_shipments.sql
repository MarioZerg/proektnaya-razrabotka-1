-- Причина отказа при приёме заявки в цехе: сотрудник цеха может отказать в приёме (состав
-- заявки не в порядке), заявка остаётся в статусе "Отправлено" с указанной причиной, пока
-- кладовщик/администратор не внесёт исправления через экран сборки.
ALTER TABLE shipments ADD COLUMN IF NOT EXISTS reject_reason TEXT NULL;