-- Временная проверка: ставим сотруднику просроченный срок, чтобы убедиться,
-- что блокировка срабатывает. Значение снимается следующей миграцией.
UPDATE users SET docs_deadline = now() - interval '1 day' WHERE id = 7;
