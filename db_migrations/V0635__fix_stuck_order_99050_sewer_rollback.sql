-- ЗАКАЗ 0123451984-0088-1 (id 99050) ЗАСТРЯЛ МЕЖДУ ЭТАПАМИ.
--
-- Админ вернул его со «Стикеровки» назад, но тогда откат чистил только вкладку:
-- швея (sewer_user_id = 21), дата пошива и «кто держит заказ» остались на месте.
-- В итоге вещь числится отшитой конкретной швеёй, но лежит в очереди — назначить
-- другую нельзя, и сама очередь заказ не отдаёт: она выдаёт только ничьи вещи.
--
-- Сам механизм отката уже исправлен в backend/orders (update_order снимает
-- исполнителя, дату и неоплаченное начисление). Эта миграция чинит единственный
-- заказ, пострадавший до исправления: возвращаем его в «Раскроено» и делаем ничьим,
-- чтобы швея взяла его из очереди или админ назначил вручную.
--
-- Раскрой не трогаем: ткань разрезана, закройщик (cutter_user_id = 23) свою работу
-- сделал, и его начисление остаётся. Начислений за пошив по этому заказу нет —
-- удалять нечего.
UPDATE orders
SET sewing_status    = 'Раскроено',
    sewer_user_id    = NULL,
    sewn_at          = NULL,
    taken_at         = NULL,
    assigned_user_id = NULL,
    packer_user_id   = NULL,
    packed_at        = NULL
WHERE id = 99050;
