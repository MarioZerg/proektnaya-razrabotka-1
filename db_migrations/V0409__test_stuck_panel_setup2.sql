-- Проверка плашки «Зависли после отмены» на настоящем случае зависания.
--
-- Вещь 1081: заказ отменён на маркетплейсе, вещь НЕ закреплена ни за одним живым
-- заказом. Приводим её в состояние «в сборке с ярлыком» — ровно то зависание,
-- которое плашка обязана поймать и предложить вернуть в оборот.
--
-- Состояние временное: сразу после проверки вернём вещь на полку.
UPDATE t_p86119184_proektnaya_razrabotk.goods_warehouse
SET status = 'awaiting_supply',
    shipping_labeled_at = now()
WHERE id = 1081 AND status = 'in_stock' AND reserved_order_id IS NULL;
