-- Откат проверки: возвращаем заказ 81309 в рабочую поставку 1215.
--
-- Проверка прошла успешно: после удаления из поставки заказ ушёл в накопитель 1218
-- (очередь на отгрузку), а не пропал из всех списков, как было раньше. Кладовщик
-- увидит его в счётчике и сможет отсканировать в следующую поставку.
--
-- Временную поставку 1219 не удаляем (удаление данных запрещено) — помечаем её
-- отменённой, чтобы она не мешалась в рабочих списках кладовщика.
UPDATE t_p86119184_proektnaya_razrabotk.wb_supply_orders
SET supply_id = 1215
WHERE order_id = 81309;

UPDATE t_p86119184_proektnaya_razrabotk.marketplace_supplies
SET status = 'Отменена',
    comment = 'Служебная поставка проверки — не использовать'
WHERE id = 1219;
