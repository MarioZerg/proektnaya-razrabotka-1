-- Новый статус склада товара: «На проверке».
--
-- Владелец переописал работу склада: вещь, приехавшая возвратом с маркетплейса, сначала
-- разбирается, затем проверяется (годна ли к повторной продаже) и только потом уходит
-- на хранение. Статуса проверки в системе не было — вещи попадали на полку сразу.
COMMENT ON COLUMN t_p86119184_proektnaya_razrabotk.goods_warehouse.status IS
    'awaiting_shelf — на разборе (принят, ждёт раскладки); checking — на проверке; '
    'in_stock — на хранении; picking — на сборке (снят с полки, отстикерован); '
    'awaiting_supply — на поставку (сшит в цехе, ждёт короба); reserved — в поставке; '
    'shipped — отгружен; lost — утерян';
