-- Начинаем обход заказов OZON заново.
-- Фильтр статусов изменился: раньше брали только «ожидает сборки», теперь ещё и
-- «ожидает передачи в доставку». Пропущенные заказы лежат в начале списка,
-- поэтому сбрасываем позицию.
UPDATE system_settings SET value = '0', updated_at = now()
WHERE key = 'ozon_sync_offset';
