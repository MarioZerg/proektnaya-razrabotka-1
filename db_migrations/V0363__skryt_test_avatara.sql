-- Скрываем служебное сообщение, отправленное при проверке аватаров в чате.
-- Строку не стираем: чат вообще не удаляет сообщения, только скрывает из ленты.
UPDATE chat_messages
SET hidden_at = now()
WHERE text = 'Проверка аватара' AND hidden_at IS NULL;
