import React from 'react';
import type { LucideProps } from 'lucide-react';
import {
  AlertTriangle,
  ArrowLeft,
  ArrowLeftRight,
  ArrowRight,
  Ban,
  Banknote,
  Barcode,
  BellRing,
  Boxes,
  Building2,
  Calendar,
  CalendarClock,
  CalendarSync,
  Check,
  CheckCheck,
  CheckCircle2,
  ChevronLeft,
  ChevronRight,
  ChevronsUpDown,
  Circle,
  CircleCheck,
  ClipboardCheck,
  ClipboardList,
  Clock,
  Coins,
  Copy,
  CopyX,
  Delete,
  Download,
  ExternalLink,
  Eye,
  Factory,
  FileSignature,
  FileSpreadsheet,
  FileText,
  FileWarning,
  FlaskConical,
  HelpCircle,
  Info,
  KeyRound,
  Layers,
  LayoutDashboard,
  LayoutGrid,
  ListChecks,
  ListPlus,
  Loader2,
  Lock,
  LockOpen,
  LogOut,
  Mail,
  MailCheck,
  MapPin,
  MessageCircle,
  Minus,
  MinusCircle,
  MonitorPlay,
  Package,
  PackageCheck,
  PackageMinus,
  PackageOpen,
  PackagePlus,
  PackageSearch,
  PackageX,
  PartyPopper,
  PenLine,
  Pencil,
  Phone,
  Plus,
  Printer,
  QrCode,
  RefreshCw,
  Repeat,
  ScanLine,
  Scissors,
  Scroll,
  Search,
  Send,
  Settings,
  ShieldAlert,
  Shirt,
  ShoppingBag,
  Shuffle,
  SlidersHorizontal,
  Smartphone,
  Sparkles,
  Star,
  Tag,
  Tags,
  TimerReset,
  Trash2,
  TriangleAlert,
  Truck,
  Hand,
  Pause,
  Play,
  TrendingUp,
  Undo2,
  Upload,
  User,
  UserPlus,
  Users,
  Wallet,
  Wrench,
  X,
  Zap,
  CircleAlert,
} from 'lucide-react';

/**
 * Иконки приложения.
 *
 * ВАЖНО: иконки перечислены поимённо, а не подключаются целиком через `import *`.
 * Раньше подключалась вся библиотека — в стартовый файл попадали ВСЕ иконки (больше
 * тысячи), из-за чего сайт долго загружался на телефоне. Теперь берутся только те,
 * что реально используются.
 *
 * Добавляете новую иконку — впишите её в список ниже, иначе покажется запасная.
 */
const icons: Record<string, React.FC<LucideProps>> = {
  AlertTriangle,
  ArrowLeft,
  ArrowLeftRight,
  ArrowRight,
  Ban,
  Banknote,
  Barcode,
  BellRing,
  Boxes,
  Building2,
  Calendar,
  CalendarClock,
  CalendarSync,
  Check,
  CheckCheck,
  CheckCircle2,
  ChevronLeft,
  ChevronRight,
  ChevronsUpDown,
  Circle,
  CircleCheck,
  ClipboardCheck,
  ClipboardList,
  Clock,
  Coins,
  Copy,
  CopyX,
  Delete,
  Download,
  ExternalLink,
  Eye,
  Factory,
  FileSignature,
  FileSpreadsheet,
  FileText,
  FileWarning,
  FlaskConical,
  HelpCircle,
  Info,
  KeyRound,
  Layers,
  LayoutDashboard,
  LayoutGrid,
  ListChecks,
  ListPlus,
  Loader2,
  Lock,
  LockOpen,
  LogOut,
  Mail,
  MailCheck,
  MapPin,
  MessageCircle,
  Minus,
  MinusCircle,
  MonitorPlay,
  Package,
  PackageCheck,
  PackageMinus,
  PackageOpen,
  PackagePlus,
  PackageSearch,
  PackageX,
  PartyPopper,
  PenLine,
  Pencil,
  Phone,
  Plus,
  Printer,
  QrCode,
  RefreshCw,
  Repeat,
  ScanLine,
  Scissors,
  Scroll,
  Search,
  Send,
  Settings,
  ShieldAlert,
  Shirt,
  ShoppingBag,
  Shuffle,
  SlidersHorizontal,
  Smartphone,
  Sparkles,
  Star,
  Tag,
  Tags,
  TimerReset,
  Trash2,
  TriangleAlert,
  Truck,
  Hand,
  Pause,
  Play,
  TrendingUp,
  Undo2,
  Upload,
  User,
  UserPlus,
  Users,
  Wallet,
  Wrench,
  X,
  Zap,
  CircleAlert,
};

interface IconProps extends LucideProps {
  name: string;
  fallback?: string;
}

const Icon: React.FC<IconProps> = ({ name, fallback = 'CircleAlert', ...props }) => {
  const IconComponent = icons[name] || icons[fallback] || CircleAlert;
  return <IconComponent {...props} />;
};

export default Icon;
